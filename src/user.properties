# ≤È—Ø
getUserById = SELECT u.email,u.username,u.id,d.iconurl,d.sex,i.kindids FROM `user` u,user_detail d,user_interested i WHERE u.id = ? AND u.id = d.userid AND u.id = i.userid;
getUserByQuestionId = SELECT u.id,u.email,u.username,d.iconurl,d.sex,i.kindids,q.questionid,q.questionname FROM question q,`user` u,user_detail d,user_interested i WHERE q.questionid = ? AND q.creatorid = u.id AND u.id = d.userid AND u.id = i.userid; 
getUserListByIds = SELECT u.email,u.username,u.id,d.iconurl,d.sex,i.kindids FROM `user` u,user_detail d,user_interested i WHERE u.id = d.userid AND u.id = i.userid AND u.id in 